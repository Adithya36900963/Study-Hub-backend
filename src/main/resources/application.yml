server:
  port: 8080

spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1522/XEPDB1
    username: COLLAGE
    password: Rupa
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    dd;l-auto: update
